import { EqualpasswordDirective } from './equalpassword.directive';

describe('EqualpasswordDirective', () => {
  it('should create an instance', () => {
    const directive = new EqualpasswordDirective();
    expect(directive).toBeTruthy();
  });
});
